OPERATION SCALE

The SCALE operation resets the scale of an input overlay.

The general form of the instruction for this operation is:

       SCA[LE] overlay	 A[T] number

where individual phrases are interpreted as follows.

       SCALE overlay   gives the title of the overlay whose scale is to be
		       reset

       AT number       gives a number indicating the scale to be assigned.
